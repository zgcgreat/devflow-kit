# devflow-kit Cursor rule

For non-trivial software tasks, use `devflow-kit/SKILL.md` as the entry point. For broad requests, read `devflow-kit/flow/GO.md` and route to the correct phase. Use `.specs/<req-id>/` artifacts for durable decisions and verification evidence. Do not implement before requirements and tasks exist unless the change is explicitly tiny and self-contained.
