# SYSTEM — 永久注入版

> ⚠️ **本文件是精简注入版。真正执行流程必须读取 `flow/GO.md` 并严格按其步骤执行。**
>
> 把这个文件的内容复制进你的 IDE 全局规则文件（`.windsurfrules` / `.cursorrules` / `CLAUDE.md`），IDE 会自动注入每次会话。
> 注入后你只需要 `@flow/GO.md` 引用路由器，不用再 `@METHODOLOGY` 和 `@RULES`。

---

## 你正在协作一个使用 devflow-kit 流程的项目

**唯一入口**：`flow/GO.md` 是路由器和流程事实源。不读 GO.md 直接动手 = 违反流程。

完整流程：`需求确认 → 需求分析 → 方案设计 → [UI设计] → 任务拆分 → 开发 → 测试 → 审查 → 集成 → 归档`

每阶段产物存到 `.specs/<req-id>/`。

## 核心强制规则（摘要）

> 完整规则见 `flow/RULES.md`。以下为 R1/R2 规则摘要表：

| 规则 | SYSTEM.md（摘要） | RULES.md（完整） |
|------|-------------------|------------------|
| R1 状态管理 | 6 条摘要 | 12 条完整（R1.1~R1.12） |
| R2 阶段门 | 6 条摘要 | 10 条完整（R2.0~R2.10） |
| 模式判定 | 有摘要表 | 没有 |
| 模板强制 | 有（R13.9） | 有（R13.9/R13.10） |
| 入场检测 | R2.0 简述 | R1.11 详细 |

**关键规则**：

1. **入场检测强制**：进入 0-confirm / 1-analysis / 2-design 前必须输出检测结果框（R1.11 / R2.0）
2. **skill 加载强制**：进入任何阶段前必须加载该阶段对应的 skill（见 GO.md 路由表）
3. **模板强制**：输出产物前必须先读模板，产物必须包含模板所有段落（R13.9）；遗漏必须补齐（R13.10）
4. **阶段门验证**：缺前置产物必须回退生成，禁止伪造"已满足"（R2.7/R2.8）
5. **verify 强制**：verify 未通过禁止标记完成（R2.4）
6. **范围控制**：严禁悄悄扩大范围（R7.1）

## 角色红线

- Architect 不写实现代码
- Dev 不改 01-需求分析.md / 02-方案设计.md（发现问题开新 req）
- Reviewer 不修代码（只产报告 + 修复 task）

## 违规处理

检测到即将违反规则时，输出：
` 规则 R{编号} 触发：<原因>。需要人工决策。`
然后停下等待，禁止"自我授权"绕开规则。

---

> 完整方法论见 `flow/METHODOLOGY.md`，完整规则见 `flow/RULES.md`，路由器见 `flow/GO.md`。
