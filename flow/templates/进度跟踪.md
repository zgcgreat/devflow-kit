# STATE — 跨会话项目状态

> `.specs/进度跟踪.md`。AI 每次开始新会话先读这个，结束前更新。

---

## 当前位置

- **活跃 Req**: `<req-id>` 或 "无"
- **当前阶段**: 需求确认 / ANALYSIS / DESIGN / TASK / DEV / TEST / REVIEW / INTEGRATION / 无
- **当前 Task**: `<T03>` 或 "—"
- **中断任务**（来自 R1.5 清窗）: `<task-id>` 或 "无"
  - 若非空，下一会话必须先加载 `@.specs/<req-id>/<task-id>-开发中断快照.md`
- **会话开始建议**:
  - 若有「中断任务」：按 R1.5 清窗后加载顺序恢复
  - 若有活跃 req：先读 `@.specs/<req-id>/` 下相关产物
  - 若无：等用户给新需求

---

## 🔄 压缩恢复指引（会话压缩后 AI 必读）

> ⚠️ **当会话被压缩恢复后，AI 必须先读本节，按步骤恢复上下文，不能假装从未中断。**

### 恢复步骤（必须按顺序执行）

```
Step 1: 读本文件（进度跟踪.md）
   └─ 获取：活跃 Req、当前阶段、当前 Task、模式、产物清单

Step 2: 读当前阶段的产物文件
   └─ 按下方「当前模式 → 阶段进度」找到已完成的最后一个阶段
   └─ 读该阶段的产物文件，恢复上下文

Step 3: 输出恢复摘要（必须）
   ┌─────────────────────────────────────────────────────────────┐
   │  🔄 会话恢复摘要                                            │
   ├─────────────────────────────────────────────────────────────┤
   │  恢复自：<产物文件名>                                       │
   │  当前阶段：<阶段名>                                         │
   │  当前任务：<Task ID 或 "—" >                                │
   │  模式：<Fast/Standard/Strict>                               │
   │  已完成阶段：<列出已打钩的阶段>                              │
   │  下一步：<具体下一步动作>                                    │
   └─────────────────────────────────────────────────────────────┘

Step 4: 继续执行
   └─ 按「下一步」继续，不跳过任何阶段
```

### 恢复时必须加载的文件（按阶段）

| 当前阶段 | 必须加载的文件 |
|----------|----------------|
| 需求确认 | `进度跟踪.md` |
| ANALYSIS | `00-需求确认.md` |
| DESIGN | `00-需求确认.md` + `01-需求分析.md` + `上下文.md` |
| TASK | `02-方案设计.md` + [`02a-UI设计.md`] |
| DEV | `03-任务拆分.md`（当前 task 块）+ `02-方案设计.md` ## 0 段 |
| TEST | `03-任务拆分.md` + `04-开发记录.md` + 各 `*-开发记录.md` |
| REVIEW | `05-测试报告.md` + `git diff` |
| INTEGRATION | 全部产物 + `06-代码审查.md` |

### 恢复后禁止的行为

- ❌ 假装从未中断，不输出恢复摘要
- ❌ 跳过已完成的阶段重新做
- ❌ 忽略产物清单，重新生成已存在的产物
- ❌ 改变已确定的模式（除非用户明确要求）
- ❌ 忘记当前任务的中断点，从头开始

---

## 当前模式

- **模式**: `<Fast / Standard / Strict>`
- **判定理由**: `<为什么是这个模式>`
- **判定时间**: `<YYYY-MM-DD HH:mm>`

### 流程与产物清单

> 根据选定模式，以下是需要完成的阶段和产物。每完成一个阶段打钩。

#### Fast 模式

```
流程：短任务说明 → 修改 → 窄验证 → 结果证据
产物：开发记录.md（可选）

阶段进度：
- [ ] 短任务说明
- [ ] 修改
- [ ] 窄验证
- [ ] 结果证据
```

#### Standard 模式

```
流程：需求确认 → 需求分析 → 方案设计 → [2a UI设计]* → 任务拆分 → 开发 → 测试 → 审查 → 集成
产物：00-需求确认.md → 01-需求分析.md → 02-方案设计.md → [02a-UI设计.md] → 03-任务拆分.md → *-开发记录.md → 05-测试报告.md → 06-代码审查.md

阶段进度：
- [ ] 需求确认 → 00-需求确认.md
- [ ] 需求分析 → 01-需求分析.md
- [ ] 方案设计 → 02-方案设计.md
- [ ] 2a UI设计 → 02a-UI设计.md（仅前端项目）
- [ ] 任务拆分 → 03-任务拆分.md
- [ ] 开发 → *-开发记录.md
- [ ] 测试 → 05-测试报告.md
- [ ] 审查 → 06-代码审查.md
- [ ] 集成 → 07-发布清单.md
```

#### Strict 模式

```
流程：完整流程 + 安全审查 + 迁移方案 + 回滚方案
产物：Standard 全部 + 安全审查报告 + 迁移方案 + 回滚方案

阶段进度：
- [ ] 需求确认 → 00-需求确认.md
- [ ] 需求分析 → 01-需求分析.md
- [ ] 方案设计 → 02-方案设计.md
- [ ] 2a UI设计 → 02a-UI设计.md（仅前端项目）
- [ ] 任务拆分 → 03-任务拆分.md
- [ ] 开发 → *-开发记录.md
- [ ] 测试 → 05-测试报告.md
- [ ] 审查 → 06-代码审查.md
- [ ] 安全审查 → 安全审查报告
- [ ] 迁移方案 → 迁移方案.md（如有 schema 变更）
- [ ] 回滚方案 → 回滚方案.md
- [ ] 集成 → 07-发布清单.md
```

> *2a UI设计 仅前端项目需要

---

## 阻塞与待决策

| 项 | 类型 | 详情 | 待谁 | 自 |
|---|---|---|---|---|
|  | bug / 决策 / 范围 |  | 用户 |  |

## 决策日志（最近 10 条，倒序）

- `[2026-04-30]` <一句话决策> — `@.specs/<req-id>/02-方案设计.md#D2`
- ……

## 已归档需求（最近 5 个，倒序）

| 日期 | Req | 摘要 | PR |
|---|---|---|---|
| 2026-04-30 | add-dark-mode | 主题切换支持 + 持久化 | #42 |

---

## 横向命令状态（GO.md / 各 prompt 会读这里决定要不要主动提示）

```yaml
# 老项目入场扫描 / AI 上下文文档替代
ai_context_doc: 上下文.md          # 或 AGENTS.md / CLAUDE.md / 用户指定路径 / none
last_intel_scan: 2026-04-15          # 上次跑 I-intel-scan 的日期；为空表示从未跑
                                     # GO.md 第二步会按 90 天阈值提醒重扫

# 架构演进
last_architect_at: 2026-03-01        # 上次跑 A-architect 的日期；首跑后填
last_evolve_at: 2026-04-20           # 上次跑 A-evolve 的日期；用于过滤扫描范围
last_evolve_promoted:                # 上次 A-evolve 已处理的 req-id 列表，避免重扫
  - add-cache-layer
  - add-bg-jobs
  - add-notifications

# 健康巡检
last_health_at: 2026-04-10           # 上次跑 M-health 的日期
last_health_score: 78                # 上次综合分（用于本次对比）
```

字段含义：

- 读这块只为**决定要不要在 GO.md 路由时主动提醒**（如"距上次 evolve 已 60 天，建议同步"）
- 不影响主流程，缺字段就当未跑过对应工作流
- 各横向工作流跑完会自己更新这里

---

## 健康检查（AI 每次会话开始前自检）

- [ ] `.specs/` 目录存在
- [ ] 当前活跃需求的工件文件齐全（按当前阶段）
- [ ] 没有未归档但已合并的需求
- [ ] 没有 > 3 轮自动重试仍未解决的失败
